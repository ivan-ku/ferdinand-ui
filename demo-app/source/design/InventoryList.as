package design {
import ferdinand.create.Base;
import ferdinand.create.Block;

public class InventoryList extends Block {
    public function InventoryList(base:Base) {
        super(base);
    }
}
}

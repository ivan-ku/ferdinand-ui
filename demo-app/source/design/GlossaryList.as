package design {
import ferdinand.create.Base;
import ferdinand.create.Block;

// Example of reusable control implementation using Ferdinand AS3-mode
public class GlossaryList extends Block {

    public function GlossaryList(base:Base) {
        super(base);
        // TODO: setup structure
    }
}
}
